Developer notes

- This repo intentionally ships with placeholder sign assets.
- Replace generated GIFs in `signaction_assets/` with real sign clips/GIFs using the same filenames (token-based).
- For ASL datasets (WLASL, RWTH, etc.), build a preprocessing script to extract per-word clips and name them as `TOKEN.gif`.
