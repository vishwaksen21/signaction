Assets

This folder is intentionally empty in git.

Runtime assets are generated/placed into `signaction_assets/` at the repo root.

To use real sign assets:
- Put per-token GIFs or MP4s in `signaction_assets/`.
- Filenames must match tokens produced by the NLP step (uppercase), e.g. `HELLO.gif`.
